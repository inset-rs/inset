//! Flutter counterpart: `services/text_editing.dart`.
//!
//! [`TextSelection`] lives in `reveal-embedder` because [`reveal_embedder::View`]
//! names [`reveal_embedder::TextEditingValue`].

pub use reveal_embedder::TextSelection;
