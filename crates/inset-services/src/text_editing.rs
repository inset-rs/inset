//! Flutter counterpart: `services/text_editing.dart`.
//!
//! [`TextSelection`] lives in `inset-embedder` because [`inset_embedder::View`]
//! names [`inset_embedder::TextEditingValue`].

pub use inset_embedder::TextSelection;
